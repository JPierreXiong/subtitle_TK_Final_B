# Subtitle TK 软件功能清单与数据库迁移方案

## 📋 软件功能清单

### 一、核心业务功能

#### 1. 视频字幕提取与翻译系统
- **YouTube 视频处理**
  - 视频链接解析
  - 字幕提取（SRT 格式）
  - 视频元数据提取（标题、作者、点赞、观看、分享、发布时间）
  
- **TikTok 视频处理**
  - 视频链接解析
  - 字幕提取（SRT 格式）
  - 视频下载（无水印）
  - 视频元数据提取
  - R2 存储（24小时有效期）

- **AI 翻译功能**
  - 使用 Gemini AI 进行翻译
  - 支持 12+ 种语言
  - 单次翻译和分片翻译
  - 翻译结果清理和格式化

- **文件导出**
  - SRT 字幕文件下载
  - CSV 格式导出
  - 视频文件下载（TikTok）

#### 2. 任务管理系统
- **任务提交流程**
  - 异步任务处理
  - 状态跟踪（pending → processing → extracted → translating → completed）
  - 进度显示（0-100%）
  - 错误处理和重试机制

- **历史记录**
  - 任务历史查询
  - 分页支持
  - 过期检测
  - 批量下载

#### 3. 积分与支付系统
- **积分管理**
  - 积分授予（支付、订阅、签到、赠送）
  - 积分消费（任务处理、AI生成）
  - FIFO 队列消费机制
  - 积分过期管理
  - 积分余额查询

- **支付集成**
  - Creem 支付（主要）
  - Stripe 支付（可选）
  - PayPal 支付（可选）
  - 订阅管理
  - 订单管理
  - Webhook 回调处理

- **订阅计划**
  - 免费计划（free）
  - 基础计划（base）
  - 专业计划（pro）
  - 按需计划（on_demand）
  - 计划限制（视频时长、并发数、导出格式、存储时长、翻译字符限制）

#### 4. 用户系统
- **用户认证**
  - 邮箱密码登录
  - Google OAuth 登录
  - GitHub OAuth 登录
  - 一键登录（Google One Tap）
  - 会话管理
  - 邮箱验证

- **用户管理**
  - 用户信息管理
  - 头像上传
  - 计划类型管理
  - 免费试用管理

#### 5. 每日签到系统
- **签到功能**
  - 每日签到
  - 签到状态查询
  - 签到奖励（积分）
  - 防重复签到

### 二、AI 功能

#### 1. AI 生成服务
- **图片生成**
  - 文本生成图片（text-to-image）
  - 图片转图片（image-to-image）
  - 支持多种 AI 提供商（OpenRouter、Replicate、Fal、Kie）

- **音乐生成**
  - 文本生成音乐（text-to-music）

#### 2. AI 聊天功能
- **聊天系统**
  - 多轮对话
  - 对话历史
  - 消息管理
  - 支持多种 AI 模型

### 三、内容管理系统

#### 1. 内容管理
- **文章管理**
  - 文章创建、编辑、删除
  - 分类和标签
  - 状态管理（草稿、发布）
  - 排序功能

- **分类管理**
  - 分类层级结构
  - 分类图标和图片
  - 状态管理

#### 2. 推荐系统
- **用户推荐**
  - 推荐提交
  - 审核流程（待审核、已批准、已拒绝）
  - 多语言支持（en、zh、fr）
  - 评分系统（1-5星）
  - 排序功能

### 四、权限管理系统（RBAC）

#### 1. 角色管理
- **角色定义**
  - 管理员（admin）
  - 编辑（editor）
  - 查看者（viewer）
  - 角色状态管理

#### 2. 权限管理
- **权限控制**
  - 资源权限（users、posts、categories等）
  - 操作权限（read、write、delete）
  - 角色权限关联
  - 用户角色分配

### 五、API 系统

#### 1. API 密钥管理
- **API 密钥**
  - 密钥生成
  - 密钥管理
  - 密钥状态管理（active、inactive）
  - API 调用认证

#### 2. API 售卖
- **API 服务**
  - API 调用计费
  - 积分扣除
  - 使用统计

### 六、存储系统

#### 1. 存储提供商支持
- **Vercel Blob**（推荐）
- **Cloudflare R2**
- **AWS S3**

#### 2. 存储功能
- **文件上传**
  - 图片上传
  - 视频上传（流式）
  - 预签名 URL 生成

### 七、系统功能

#### 1. 配置管理
- **动态配置**
  - 环境变量配置
  - 数据库配置
  - 运行时配置更新

#### 2. 多语言支持
- **国际化**
  - 英语（en）
  - 中文（zh）
  - 法语（fr）
  - 动态语言切换

#### 3. 主题系统
- **主题管理**
  - 默认主题
  - 外观模式（system、light、dark）
  - 主题切换

---

## 🔄 数据库迁移方案：Neon → Supabase

### 一、迁移概述

#### 1.1 迁移目标
- **从**: Neon PostgreSQL
- **到**: Supabase PostgreSQL
- **原则**: 保持 ShipAny 框架结构不变，仅更换数据库连接

#### 1.2 兼容性分析
✅ **完全兼容**
- Neon 和 Supabase 都使用标准 PostgreSQL
- 使用相同的 Drizzle ORM
- 使用相同的 `postgres` 客户端库
- Schema 定义无需修改
- 查询语句无需修改

#### 1.3 当前状态检查
根据 `.env.local` 文件，**当前已配置 Supabase 连接**：
```
DATABASE_URL=postgres://postgres.qeqgoztrtyrfzkgpfhvx:...@aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
DATABASE_PROVIDER=postgresql
```

### 二、迁移步骤

#### 2.1 准备工作

##### 步骤 1: 备份 Neon 数据库
```bash
# 使用 pg_dump 备份 Neon 数据库
pg_dump "neon_connection_string" > neon_backup.sql

# 或者使用 Neon 控制台导出功能
```

##### 步骤 2: 创建 Supabase 项目
1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 创建新项目（如果还没有）
3. 记录项目连接信息：
   - Connection Pooling URL（推荐用于生产环境）
   - Direct Connection URL（用于迁移和开发）

##### 步骤 3: 验证 Supabase 连接
```bash
# 测试连接
psql "supabase_connection_string"

# 或者使用 Drizzle Studio
pnpm db:studio
```

#### 2.2 数据迁移

##### 步骤 4: 导出 Neon 数据
```bash
# 导出所有表数据（不包括 schema）
pg_dump "neon_connection_string" \
  --data-only \
  --no-owner \
  --no-privileges \
  > neon_data.sql
```

##### 步骤 5: 在 Supabase 中创建 Schema
```bash
# 使用 Drizzle Kit 推送 schema（推荐）
pnpm db:push

# 或者使用迁移文件
pnpm db:migrate
```

##### 步骤 6: 导入数据到 Supabase
```bash
# 导入数据
psql "supabase_connection_string" < neon_data.sql

# 或者使用 Supabase Dashboard 的 SQL Editor
```

#### 2.3 配置更新

##### 步骤 7: 更新环境变量
**开发环境** (`.env.local`):
```env
# Supabase Connection Pooling（推荐用于生产）
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true

# 或者 Direct Connection（用于迁移和开发）
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@db.[project-ref].supabase.co:5432/postgres?sslmode=require

DATABASE_PROVIDER=postgresql
DB_SINGLETON_ENABLED=false  # Supabase 推荐使用连接池，不需要单例模式
```

**生产环境** (Vercel/Cloudflare):
- 在部署平台的环境变量中更新 `DATABASE_URL`
- 使用 Connection Pooling URL（端口 6543）

##### 步骤 8: 验证连接
```bash
# 重启开发服务器
pnpm dev

# 检查数据库连接
# 访问应用，测试基本功能
```

#### 2.4 功能验证

##### 步骤 9: 功能测试清单
- [ ] 用户注册/登录
- [ ] 视频任务提交
- [ ] 字幕提取
- [ ] 翻译功能
- [ ] 积分系统
- [ ] 支付流程
- [ ] 历史记录查询
- [ ] API 密钥管理
- [ ] 管理员功能

### 三、技术细节

#### 3.1 连接配置差异

**Neon 连接特点**:
- 使用标准 PostgreSQL 连接
- 支持 Serverless 连接
- 自动扩展

**Supabase 连接特点**:
- 提供两种连接方式：
  1. **Connection Pooling** (端口 6543)
     - 使用 PgBouncer
     - 适合生产环境
     - 连接数限制较高
  2. **Direct Connection** (端口 5432)
     - 直接连接 PostgreSQL
     - 适合迁移和开发
     - 连接数限制较低

#### 3.2 代码变更要求

**✅ 无需修改的文件**:
- `src/core/db/index.ts` - 数据库连接逻辑（已支持标准 PostgreSQL）
- `src/core/db/config.ts` - Drizzle 配置（已支持 PostgreSQL）
- `src/config/db/schema.ts` - Schema 定义（无需修改）
- 所有模型文件 - 查询逻辑无需修改
- 所有 API 路由 - 业务逻辑无需修改

**⚠️ 注意事项**:
1. **连接池配置**: Supabase 推荐使用 Connection Pooling，代码中已支持
2. **SSL 模式**: Supabase 要求 `sslmode=require`，已在连接字符串中配置
3. **单例模式**: Supabase 使用连接池，建议 `DB_SINGLETON_ENABLED=false`

#### 3.3 性能优化建议

1. **使用 Connection Pooling**
   ```env
   # 生产环境使用 Pooling URL
   DATABASE_URL=postgresql://...@pooler.supabase.com:6543/...
   ```

2. **连接池参数**
   - Supabase 自动管理连接池
   - 无需手动配置 `max` 连接数
   - 代码中的连接配置已适配

3. **查询优化**
   - 使用索引（Schema 中已定义）
   - 避免 N+1 查询
   - 使用分页查询

### 四、回滚方案

#### 4.1 如果迁移失败

1. **恢复环境变量**
   ```env
   DATABASE_URL=neon_original_connection_string
   ```

2. **验证连接**
   ```bash
   pnpm dev
   ```

3. **检查数据完整性**
   - 验证关键数据表
   - 检查用户数据
   - 验证订单和积分记录

### 五、迁移检查清单

#### 5.1 迁移前
- [ ] 备份 Neon 数据库
- [ ] 创建 Supabase 项目
- [ ] 记录 Supabase 连接信息
- [ ] 测试 Supabase 连接
- [ ] 准备回滚方案

#### 5.2 迁移中
- [ ] 导出 Neon 数据
- [ ] 在 Supabase 中创建 Schema
- [ ] 导入数据到 Supabase
- [ ] 更新环境变量
- [ ] 验证数据完整性

#### 5.3 迁移后
- [ ] 功能测试（所有核心功能）
- [ ] 性能测试
- [ ] 监控错误日志
- [ ] 更新生产环境配置
- [ ] 通知团队

### 六、Supabase 特定功能（可选）

#### 6.1 Supabase 额外功能
如果未来需要使用 Supabase 特定功能，可以考虑：
- **Realtime**: 实时数据同步
- **Storage**: 文件存储（可替代 R2/S3）
- **Auth**: 认证服务（当前使用 Better Auth）
- **Edge Functions**: 边缘函数

**注意**: 这些功能需要修改 ShipAny 结构，需要先提出方案并获得批准。

### 七、总结

#### 7.1 迁移优势
✅ **零代码变更**: 完全兼容，无需修改任何代码
✅ **快速迁移**: 仅需更新环境变量
✅ **数据安全**: 标准 PostgreSQL，数据格式一致
✅ **性能提升**: Supabase 连接池优化

#### 7.2 注意事项
⚠️ **连接字符串格式**: 确保使用正确的 Supabase 连接字符串
⚠️ **SSL 要求**: Supabase 要求 SSL 连接
⚠️ **连接池**: 生产环境使用 Connection Pooling
⚠️ **数据验证**: 迁移后务必验证数据完整性

#### 7.3 下一步行动
1. 确认 Supabase 项目已创建
2. 备份当前 Neon 数据库
3. 执行数据迁移
4. 更新环境变量
5. 进行功能测试
6. 部署到生产环境

---

## 📊 数据库表结构总览

### 核心表（19个表）

1. **user** - 用户表
2. **session** - 会话表
3. **account** - 账户表（OAuth）
4. **verification** - 验证表
5. **config** - 配置表
6. **taxonomy** - 分类表
7. **post** - 文章表
8. **order** - 订单表
9. **subscription** - 订阅表
10. **credit** - 积分表
11. **apikey** - API 密钥表
12. **role** - 角色表
13. **permission** - 权限表
14. **role_permission** - 角色权限关联表
15. **user_role** - 用户角色关联表
16. **ai_task** - AI 任务表
17. **chat** - 聊天表
18. **chat_message** - 聊天消息表
19. **media_tasks** - 媒体任务表（核心业务表）
20. **daily_checkins** - 每日签到表
21. **testimonial** - 推荐表
22. **video_cache** - 视频缓存表

### 索引统计
- 所有表都定义了适当的索引
- 复合索引用于优化查询性能
- 唯一索引确保数据完整性

---

**文档创建时间**: 2024-12-25
**状态**: ✅ 方案已制定，等待批准执行
