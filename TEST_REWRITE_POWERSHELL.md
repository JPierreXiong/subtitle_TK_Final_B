# 🔧 PowerShell 测试改写功能（Windows）

**问题**: PowerShell 将 `<taskId>` 中的 `<` 解释为重定向操作符

---

## ✅ 正确的命令格式

### 方法 1: 使用引号（推荐）

```powershell
# 用引号包裹 taskId
pnpm tsx scripts/test-rewrite-feature.ts "your-task-id-here" viral zh-CN

# 示例
pnpm tsx scripts/test-rewrite-feature.ts "c6034957-39ad-4e8d-ad7b-cdadbd0427d2" viral zh-CN
```

### 方法 2: 直接使用实际 taskId

```powershell
# 直接替换，不使用 < > 符号
pnpm tsx scripts/test-rewrite-feature.ts abc123-def456-ghi789 viral zh-CN
```

---

## 📋 完整测试流程

### 步骤 1: 创建测试任务

**通过前端**:
1. 访问: http://localhost:3000/ai-media-extractor
2. 输入 URL: `https://www.tiktok.com/@the_shortcut_tsar/video/7415746564376530950`
3. 点击提交
4. 等待提取完成

**获取 taskId**:
- 打开浏览器开发者工具（F12）
- Network 标签 → 找到 `/api/media/submit` 请求
- 从响应中复制 `taskId`

### 步骤 2: 运行测试脚本

```powershell
# 替换 "your-task-id" 为实际的 taskId
pnpm tsx scripts/test-rewrite-feature.ts "your-task-id" viral zh-CN
```

### 步骤 3: 查看结果

测试脚本会自动：
- ✅ 检查任务状态
- ✅ 触发改写任务
- ✅ 等待结果（最多 2 分钟）
- ✅ 显示改写结果（英文母本 + 目标语言）

---

## 🐛 常见错误

### 错误 1: `<` 运算符错误

**错误信息**:
```
"<"运算符是为将来使用而保留的。
```

**原因**: PowerShell 将 `<` 解释为重定向操作符

**解决方案**: 使用引号包裹 taskId
```powershell
# ❌ 错误
pnpm tsx scripts/test-rewrite-feature.ts <taskId> viral zh-CN

# ✅ 正确
pnpm tsx scripts/test-rewrite-feature.ts "taskId" viral zh-CN
```

### 错误 2: 任务不存在

**错误信息**:
```
Task not found
```

**解决方案**: 
- 检查 taskId 是否正确
- 确保任务已创建
- 检查任务是否属于当前用户

### 错误 3: 任务状态不正确

**错误信息**:
```
Task is not ready for rewriting. Current status: pending
```

**解决方案**:
- 等待任务提取完成（状态变为 `extracted` 或 `completed`）
- 确保任务有 `subtitleRaw` 内容

---

## 💡 快速测试命令

```powershell
# 1. 创建任务（通过前端或 API）
# 2. 获取 taskId
# 3. 运行测试（替换 YOUR_TASK_ID）
pnpm tsx scripts/test-rewrite-feature.ts "YOUR_TASK_ID" viral zh-CN
```

---

## 📊 预期输出

```
═══════════════════════════════════════════════════════════
🧪 测试改写功能
═══════════════════════════════════════════════════════════

📋 测试配置:
   API URL: http://localhost:3000
   Task ID: your-task-id
   Style: viral
   Target Language: zh-CN

🔍 1. 检查任务状态...

✅ 任务状态检查成功
   状态: extracted
   进度: 100%
   是否有字幕: ✅ 是

🚀 2. 触发改写任务...

✅ 改写任务已启动
   响应状态: 202 Accepted
   消息: Rewrite task started. Results will be available via Realtime updates.

⏳ 3. 等待改写完成（最多 2 分钟）...

✅ 改写完成！（耗时: 45 秒）

📊 4. 改写结果:

═══════════════════════════════════════════════════════════
📝 英文母本 (English Master):
═══════════════════════════════════════════════════════════
1
00:00:01,000 --> 00:00:03,000
Strong opening hook...

═══════════════════════════════════════════════════════════
📝 目标语言版本 (ZH-CN Localized):
═══════════════════════════════════════════════════════════
1
00:00:01,000 --> 00:00:03,000
吸引人的开头...

✅ 测试通过！改写功能正常工作。
```

---

**PowerShell 测试指南创建时间**: 2026-01-17
